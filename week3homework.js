// Array of toppings
const pizzaToppings = ["pepperoni", "jalapenos", "bacon", "pineapple"];

// Customer greeting function
function greetCustomer() {
  console.log(`Welcome to Tyler's Pizza, we now have toppings such as; `);
  for (let topping of toppings) {
    greeting += `${topping}`;
  }
  console.log(greeting);
}
greetCustomer();

// getPizzaOrder Function
function getPizzaOrder(size, crust, ...toppings) {
  for (let toppings of pizzaToppings) {
    console.log(`One ${pizzaObject.size} ${pizzaObject.crust} pizza with ${pizzaObject.toppings} coming up!`);
  };
return pizzaOrder = {
  size:orderSize,
  crust:orderCrust,
  toppings: orderToppings,
};

// preparePizza function
function preparePizza([orderSize, orderCrust, orderToppings]);
  console.log(`One ${large} ${crust} pizza with ${toppings} is now in the oven!`);
  return preparePizza;
}

// servePizza function
function servePizza(pizza) {
  for (let toppings of pizzaToppings) {
    console.log("Order up! Here's your ${pizzaObject.size} ${pizzaObject.crust} pizza with ${pizzaObject.toppings}, enjoy!");
  }
return servePizza;

